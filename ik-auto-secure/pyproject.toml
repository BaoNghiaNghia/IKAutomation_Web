[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ik-chrome-auto"
version = "2.0.0"
description = "Privacy-first multi-profile Chrome manager for ik.playfun.vn"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
  "playwright>=1.54,<2",
  "PySide6>=6.8,<7",
  "PySide6-Fluent-Widgets>=1.7,<2",
  "qrcode[pil]>=8,<9",
  "winrt-Windows.Security.Credentials.UI>=3.2,<4; sys_platform == 'win32'",
  "winrt-Windows.Foundation>=3.2,<4; sys_platform == 'win32'",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<10",
  "ruff>=0.9,<1",
]

[project.scripts]
ik-chrome-auto = "ik_chrome_auto.__main__:main"
ik-chrome-doctor = "ik_chrome_auto.doctor:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
ik_chrome_auto = ["assets/*.ico", "assets/*.png", "assets/*.ttf", "assets/*.txt"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
